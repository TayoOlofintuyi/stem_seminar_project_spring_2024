class ScheduleBuilder {
  constructor() {
    
  }

  build(bulletin, takenCourses = [], creditHourMin = 12, creditHourMax = 18, pursuingGrad = false, csStudent = true) {

    let schedule = new Schedule();

    // given the bulletin with a dictionary, courseList {course: priority}, and takenCourses, return a schedule that is within credit hours and has the highest priority courses 

    // (ignore pursuingGrad and csStudent for now until it becomes relevant)


  }
}